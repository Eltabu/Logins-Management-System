<ContentDialog
    x:Class="LoginsManagementSystem.View.SignInView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LoginsManagementSystem.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SIGN IN"
    PrimaryButtonText="sign in"  
    SecondaryButtonText="cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick" Background="{ThemeResource AppBarBackgroundThemeBrush}">

    <!-- Content body -->
    <StackPanel x:Name="MainPanel" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Width="317">
        
        <TextBox x:Name="userNameTextBox" Header="User name"/>
        <PasswordBox x:Name="passwordTextBox" Header="Password" />
        <CheckBox x:Name="saveUserNameCheckBox" Content="Save user name"/>

        <TextBlock x:Name="errorTextBlock" CompositeMode="SourceOver" />

        
        <TextBlock Text="Lorem ipsum dolor sit amet, consectetur adipisicing elit."  TextWrapping="Wrap" />
    </StackPanel>
</ContentDialog>
